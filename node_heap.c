#include node_heap.h
#include <stdio.h>
#include <stdlib.h>

#static compareNode

int compareNode(Node parent, Node child)
{
  
}

void sift_up( Heap * heap, Node node)
{
  
}

void sift_down( Heap * heap, Node node)
{
  
}

int read_symbols( size_t maxcount, Symbol syms[] )
{
  
}

void heap_init( Heap * heap )
{
  
}

void heap_make( Heap * heap, size_t length, Symbol symlist[] )
{
  
}

void heap_add( Heap * heap, Node node )
{
  
}

Node heap_remove( Heap * heap )
{
  
}
